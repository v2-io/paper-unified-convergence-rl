## Introduction ^sec-introduction

Non-stationary reinforcement learning has matured along four largely separate research tracks. Each has produced rigorous results; none has been composed with the others into a single convergence theory.

The four tracks are visible in the recent literature as parallel lineages. **Variation-budget dynamic regret under drift** \cite{cheung-2020-reinforcement, wei-luo-2021-blackbox, mao-2021-nearoptimal, gajane-2019-variational} gives sublinear dynamic regret under bounded total variation in reward and transition dynamics. **Two-term regret decompositions** \cite{li-zhao-zhou-2024-dynamic-regret, fei-2020-dynamic, stradi-2024-learning} split the dynamic regret along an *exploration-vs-adaptation* axis, isolating the cost of confidence-set construction from the cost of tracking a moving optimum. **Tempo and forgetting analyses** \cite{lee-2023-prost-tempo, lee-2024-pausing, touati-2020-efficient, russac-2019-weighted, garivier-2008-upperconfidence} make policy-update timing or evidence-discount rate an explicit convergence variable. **Causal and interventional access** \cite{zhang-2016-mdps, zhang-2022-online-rl, lu-2021-causal, lu-2022-efficient, wang-2021-provably, zhang-2020-designing} uses causal-graph structure to sharpen sample complexity, but operates in stationary settings only.

The information-theoretic regret literature \cite{russo-2014-informationtheoretic, russo-vanroy-2014-ids, lu-2019-informationtheoretic, min-russo-2023-nonstat-bandit, lattimore-2021-mirror, kakade-2020-information-online} traverses these tracks at a different layer, using entropy, mutual information, information ratio, Pinsker, or Hellinger to bound regret. Notably absent from that literature, as we document below ([[#^sec-prior-art]]), is an *exact* reverse-KL / total-variation identity at the deterministic-optimum corner: a sharp action-distribution regret coordinate that strictly improves both Pinsker and the Bretagnolle–Huber inequality \citep{bretagnolle-huber-1978-densities} at this point. The Bretagnolle–Huber inequality is itself absent from the retrieved RL/non-stationary corpus *as an upper-bound regret coordinate* (its use in lower-bound proofs via Le Cam's method \cite{tsybakov-2009-nonparametric, lattimore-2020-bandit} is well established and not contested here); the *exact identity at its point-mass corner* — which we establish — is *a fortiori* absent.

These four tracks share an evident common ancestor — the dynamic-regret analysis of online MDPs — but no published framework composes them. In particular, no framework combines (a) a regret decomposition along the *goal-feasibility-vs-policy-quality* axis (distinct from the exploration-vs-adaptation decompositions); (b) an exact point-mass reverse-KL/TV regret identity under deterministic optimum, strictly tighter than the Bretagnolle–Huber inequality at this corner; (c) a structural survival inequality threading rate-of-policy-revision against environment-side disturbance; and (d) a closed-loop causal-access argument that makes the regret bound *learnable* on-policy rather than merely provable analytically. The fundamental question remains open:

> *Can we design a unified convergence theory for non-stationary reinforcement learning that handles non-stationarity, has explicit metric structure on policy space, and is learnable from on-policy data?*

In this paper we present the first composition that delivers all three properties jointly. Composing four structural elements — a two-gap diagnostic, a point-mass reverse-KL/TV identity, a multi-factor strategic tempo with forgetting prerequisite, and closed-loop interventional access — yields a cumulative dynamic regret theorem with rate
$$\mathrm{DynReg}(T) \;\le\; \tilde O\bigl(N_h \sqrt{(B_T+1) \, T}\bigr)$$
in the piecewise-stationary regime, where $B_T$ counts optimum-change events along the realized trajectory and $N_h$ is the planning horizon. The per-round coordinate of this rate is sharper than both Pinsker and Bretagnolle–Huber at the deterministic-π* corner. The technical anchor is an exact algebraic identity:
$$\boxed{\;\operatorname{TV}(\delta_{a^*}, Q) \;=\; 1 - e^{-D_{\mathrm{KL}}(\delta_{a^*} \,\|\, Q)},\;}$$
which holds for any policy $Q$ with $Q(a^*) > 0$ and lies *strictly below* both the Pinsker envelope $\sqrt{D_{\mathrm{KL}}/2}$ and the Bretagnolle–Huber envelope $\sqrt{1 - e^{-D_{\mathrm{KL}}}}$ at this corner.

**Roadmap.** [[#^sec-related-work]] situates the four-track landscape. [[#^sec-preliminaries]] sets minimal preliminaries. [[#^sec-main-result]] presents the four-component composition theorem with narrative unpacking and a defense of why each component is necessary. [[#^sec-mechanism]] sketches the proof via four key lemmas chained through a block decomposition. [[#^sec-conclusion]] gives concluding observations and practitioner takeaways. Full proofs and auxiliary mathematical material are in the appendices.

### Contribution ^sec-contribution

The contribution has the shape — recognized by the NeurIPS Theory Track guidelines as a valid form of originality — of *"a novel combination of existing techniques [where] the reasoning behind this combination is well-articulated."* Three specific moves carry the paper.

**(i) Point-mass reverse-KL/TV regret identity ([[#^sec-main-result]], full development [[#^sec-key-lemma-1]]).** Under deterministic optimum policy $\pi^* = \delta_{a^*}$, an elementary calculation gives the *exact identity* $D_{\mathrm{KL}}(\pi^* \,\|\, Q) = -\log Q(a^*) = -\log(1 - \operatorname{TV}(\pi^*, Q))$, equivalently $\operatorname{TV}(\pi^*, Q) = 1 - e^{-D_{\mathrm{KL}}(\pi^* \,\|\, Q)}$. Substituting this into the classical Bretagnolle–Huber inequality $\operatorname{TV}(P, Q) \le \sqrt{1 - e^{-D_{\mathrm{KL}}(P \,\|\, Q)}}$ yields $1 - e^{-D_{\mathrm{KL}}} \le \sqrt{1 - e^{-D_{\mathrm{KL}}}}$, which holds *strictly* on $D_{\mathrm{KL}} > 0$ since $x < \sqrt{x}$ on $(0,1)$. At the deterministic-$\pi^*$ corner the identity supplies the *exact* TV value strictly below the BH envelope, positioning BH as the lineage the identity sits within rather than as the source it instantiates. Composed with the textbook total-variation regret bound (full development [[#^sec-key-lemma-1]]), the identity yields a tight two-sided characterization
$$\Delta_{\min}\bigl(1 - e^{-D_{\mathrm{KL}}(\pi^* \,\|\, Q)}\bigr) \;\le\; R(Q) \;\le\; V_{\max}\bigl(1 - e^{-D_{\mathrm{KL}}(\pi^* \,\|\, Q)}\bigr),$$
*strictly* below the Pinsker form $V_{\max}\sqrt{D_{\mathrm{KL}}/2}$ — at $D_{\mathrm{KL}} = 0.01$ the identity bound is $7\times$ tighter; at $D_{\mathrm{KL}} > 2$ Pinsker becomes vacuous against the trivial $V_{\max}$ envelope while the identity continues to saturate at $V_{\max}$. The deterministic-$\pi^*$ scope is not a hard wall: the identity extends *perturbatively* to $\epsilon$-greedy and softmax-regularized optima with quantified $O(\epsilon \log(1/\epsilon))$ and $O(\tau^{-1} \exp(-\Delta_{\min}/\tau))$ corrections respectively (full development in [[#^sec-key-lemma-1]] and [[#^sec-perturbative]]), recovering exact equality at $\epsilon = 0$ / $\tau \to 0$.

**(ii) Strategic tempo with forgetting prerequisite ([[#^sec-main-result]], full development [[#^sec-key-lemma-2]]).** A multi-factor strategic tempo $\mathcal T_\Sigma$ — composed per revisable policy element of an observation rate $\nu$, an identifiability coefficient $\iota$, and an update gain $\eta$ — yields a structural survival inequality
$$\mathcal T_\Sigma^{\mathrm{bn,ss}} \;:=\; \min_{(i,j) \in E} \nu_{ij} \cdot \iota_{ij} \cdot (1 - \lambda_{ij}) \;>\; \rho_\Sigma / R_\Sigma$$
in which all three factors are multiplicatively load-bearing per element, with the *bottleneck* (per-element minimum) as aggregator. The familiar single-factor form $(1 - \lambda) > \rho_\Sigma / R_\Sigma$ is recovered as a corollary under uniform-Regime-A Beta-Bernoulli normalization. A structural-class theorem identifies the *gain-decay* class $\mathcal A_{\mathrm{decay}}$ (count-accumulating Bayesian updates, observation-aggregating schemes without restart, gradient-based Robbins–Monro decaying-step methods) as one in which every agent eventually violates the threshold for any positive disturbance rate, while non-accumulating mechanisms (constant-step SA, sliding-window, bounded-memory, block-restart) face *bidirectional* ceilings. This *lifts* the tempo result of \cite{lee-2023-prost-tempo} from a single-factor hyperparameter-optimization claim to a multi-factor structural-threshold claim. The forgetting prerequisite is the strategic analog of the persistence condition in adaptive control \cite{khalil-2002-nonlinear}, with $(1-\lambda)$ playing the role of adaptive tempo per element.

**(iii) Closed-loop interventional access ([[#^sec-main-result]], full development [[#^sec-key-lemma-3]]).** With explicit grounding in Bareinboim's causal-hierarchy theorem \cite{bareinboim-correa-ibeling-icard-2022-pearl-hierarchy}, the agent in the feedback loop *generates* Pearl Level-2 (interventional) data by construction: the action $a_t$ causally precedes $o_{t+1}$, so under positivity, sequential ignorability, and a known action mechanism, the loop's data-generating process is interventional rather than associational. This makes the regret bound of (i) *learnable* from on-policy interaction in regimes with sufficient identifiability, not merely provable in principle. The argument is implicit in the action-perception-loop framing of active inference \cite{friston-2017-active-process, parr-2022-active} and the causal-RL line \cite{zhang-2016-mdps, zhang-2022-online-rl, lu-2021-causal, lu-2022-efficient, wang-2021-provably, zhang-2020-designing}; we make the Bareinboim-hierarchy connection explicit, partition usable strength of identification into three regimes (A: full; B: partial; C: observation-only), and flag honestly where the loop yields interventional *data* without yielding identified $do$-estimates.

**Connective tissue: the two-gap diagnostic ([[#^sec-main-result]]).** A separate decomposition of the regret signal — the *satisfaction gap* $\delta_{\mathrm{sat}}$ (the goal is unmet under the best available one-step improvement) versus the *control regret* $\delta_{\mathrm{regret}}$ (the gap between the best available improvement and the agent's current policy) — provides the corrective-action routing that ties (i)–(iii) together. The 2$\times$2 disambiguation routes four regimes (success, strategy problem, capability limit, both) to four distinct corrective actions, along an axis orthogonal to the exploration-vs-adaptation decompositions of \cite{li-zhao-zhou-2024-dynamic-regret, fei-2020-dynamic, stradi-2024-learning}.

**Composition.** The four together yield (Section [[#^sec-main-result]]) a non-stationarity-aware convergence theory with three properties no existing strand exhibits jointly: the bound *handles non-stationarity* (via tempo and forgetting); has *explicit metric structure on policy space* (via the point-mass reverse-KL identity); is *learnable from on-policy data* (via closed-loop interventional access). Composition with a variation-budget block argument under a restart-on-change base learner yields the cumulative dynamic regret bound stated above — sharper per-round coordinate than Pinsker/BH, with the cumulative rate sitting in the piecewise-stationary $B_T$ family of \cite{cheung-2020-reinforcement} (*not* directly comparable to the continuous-variation rate $\tilde O(SA \, V_T^{1/3} H \, T^{2/3})$ of \cite{mao-2021-nearoptimal}; the two regimes coincide up to constants under abrupt-magnitude piecewise-stationarity). Each of the four components is itself a theorem (cited or proved); the paper's contribution is the recognition that the four together close a story none of the strands closes individually, plus the cumulative-regret rate that follows from composing the per-round identity with a base-learner stationary rate.

### Scope and limitations ^sec-scope-limitations

**What "convergence" means here.** We do not claim pointwise policy convergence — under genuine non-stationarity the optimum is itself moving. By *convergence* we mean: per-round regret-coordinate convergence within stationary blocks; cumulative dynamic regret with sublinear / vanishing-average rate when $B_T = o(T)$ (Cesàro tracking $\tfrac{1}{T}\sum_t (V^*_t - V(\pi_t)) \to 0$); and ultimate boundedness of the strategic mismatch $\|\boldsymbol\delta_\Sigma\|$ under the tempo threshold. Pointwise $V(\pi_t) \to V^*$ is structurally unavailable under genuine non-stationarity; the Cesàro tracking statement is the right replacement.

**Theory only.** No experiments. The point-mass identity is mathematically airtight (a two-line direct calculation that specializes the BH family at its deterministic-$\pi^*$ corner). The empirical grounding for the strategic-tempo claim is provided indirectly through reduction to \cite{lee-2023-prost-tempo} ProST as a special case (Section [[#^sec-mechanism]]); at the steady-state sector-parameter level the reduction is rigorous.

**Canonical scope.** The composition theorem holds under deterministic optimum policy, bounded value range, isolated optimum (so $\Delta_{\min} > 0$), and a singular causal trajectory in the sense made precise in [[#^sec-preliminaries]]. The deterministic-$\pi^*$ scope extends *perturbatively* to $\epsilon$-stochastic and softmax-regularized optima ([[#^sec-key-lemma-1]]); tied-optimum is a degraded one-sided form ([[#^sec-aux-tied-softmax]]). Each axis of the canonical scope degrades cleanly outside it (we discuss the failure modes in [[#^sec-conclusion]]).

**Comparator regime.** The cumulative rate is in the piecewise-stationary $B_T$ family, not the continuous-variation $V_T$ family of \cite{mao-2021-nearoptimal}. Generalizing the per-round-identity route to continuous variation is open ([[#^sec-conclusion]]).
